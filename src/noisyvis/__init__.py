"""noisyvis — noisy evolutionary optimisation, LONs, CoLONs and STNs.

Deliberately empty of imports: this package is imported by every entry point, and any import here
would change module-load order and so the RNG call order the reproducibility baselines pin (plan
§7.1, R1).
"""
