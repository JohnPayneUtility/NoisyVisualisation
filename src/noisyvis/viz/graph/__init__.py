"""Graph-population modules for the shared display graph: `stn` and `lon`.

Deliberately empty of re-exports; consumers import `noisyvis.viz.graph.stn` or
`noisyvis.viz.graph.lon` directly, or reach them through the `noisyvis.viz` facade.
"""
