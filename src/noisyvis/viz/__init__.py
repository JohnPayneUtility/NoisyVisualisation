"""noisyvis.viz — figures and the shared graph pipeline.

Temporary package initialiser for Stage 10 Checkpoint A, which moves the 2D plotting tree here as
`noisyvis.viz.plots`. Deliberately empty of imports: Checkpoint D2 replaces this module with the
relocated `visualization/__init__.py` facade, once the graph population, layout, styling and trace
modules have moved.
"""
