"""noisyvis.experiments.config — Hydra config resolution for the experiment workflows (plan §5.4).

Deliberately empty of imports and re-exports, like `noisyvis.experiments` (plan §7.1, R1). Import
`primitives` or `workflows` explicitly.
"""
