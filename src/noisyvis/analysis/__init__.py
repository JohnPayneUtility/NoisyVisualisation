"""noisyvis.analysis — derived numbers computed from results and from display graphs.

A peer layer of `noisyvis.viz`, not part of it: `viz` renders, `analysis` computes. Deliberately
empty of re-exports, so every consumer imports the module it actually uses, for example
`from noisyvis.analysis.graph_stats import calculate_lon_statistics`.
"""
